export default function download(event, a, state) {
    /*
    const svg = document.getElementById('character-card-parent').children[0];
    let name = svg.querySelector('[name=name]').querySelector('input').value;
    if (!name)
        name = 'Character-Card'
    let fields = svg.querySelectorAll('.field');
    for (const field of fields.values()) {
        field.querySelector('text').textContent = field.querySelector('input').value;
        field.removeChild(field.querySelector('foreignObject'));
    }
    const base64doc = window.btoa(svg.outerHTML);
    const link = document.createElement('a');
    link.download = name + '.svg';
    link.href = 'data:image/svg+xml;base64,' + base64doc;
    link.click()
    link.remove()
    */
}